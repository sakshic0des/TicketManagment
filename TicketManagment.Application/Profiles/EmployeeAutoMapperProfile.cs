
using AutoMapper;

namespace TicketManagment.Application.Profiles
{
    public class EmployeeAutoMapperProfile : Profile
    {
        public EmployeeAutoMapperProfile()
        {
            //CreateMap<Employee, EmployeeListViewModel>()
            //     .ForMember(dest => dest.CompanyName, opt => opt.MapFrom(src => src.Company!.Name!))
            //    .ForMember(dest => dest.Password, opt => opt.MapFrom(src => src.Password!.Substring(0, 2) + "***********"));

            //CreateMap<Employee, EmployeeDetailsModelView>();
        }
    }
}
